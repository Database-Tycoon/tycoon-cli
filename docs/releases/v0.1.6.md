# Tycoon v0.1.6

_Released: TBD_

## Headline

In-flight. The cycle was opened immediately after v0.1.5 shipped to absorb the dependency-audit follow-ups surfaced during the v0.1.5 release prep, plus whatever the next round of dogfooding turns up.

## Themes (working list)

- **Dependency hygiene.** Drop the two redundant pins flagged in v0.1.5's `docs/reference/dependencies.md`:
  - `websockets==16.0` from `[server]` — pulled transitively by `uvicorn[standard]==0.46.0`; tycoon never `import websockets` (uses fastapi's `WebSocket` class).
  - `ibis-framework[duckdb]==12.0.0` from `[ask]` — pulled transitively by `nao-core==0.1.8`; zero `import ibis` sites in `src/` or `tests/`.

  Net change: 2 lines of `pyproject.toml`, 2 rows of `docs/reference/dependencies.md` (the rows go away rather than getting "redundant" notes), and `uv.lock` regenerates.

- **Optional dep refresh.** `nao-core 0.1.8 → 0.1.11` is the only direct-dep bump worth chasing inside this cycle (the other patch-level bumps available — `pydantic`, `typer`, `dbt-core`, `dagster*` — are not user-visible).

## Out of scope

- [#14][] (v0.2.0 init redesign — hard breaking change, kept separate.)

[#14]: https://github.com/Database-Tycoon/tycoon-cli/issues/14
